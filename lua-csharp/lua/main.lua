local menu = require('menu')

local function main()
    
    menu.start();
-- while true do
--     HandleKeyPress()
-- end
-- for i = 1, #folders do
--     local success, files = pcall(filesWorker.getFiles, folders[i])
--     if (success) then
--         for j = 1, #files do
--             pcall(PrintToConsole, files[j])
--         end
--     end
-- end
-- for key, folder in pairs(folders) do
--     local files = filesWorker.getFiles(folder)
--     for key, file in pairs(files) do
--         PrintToConsole(file)
--     end
-- end
end

main();
